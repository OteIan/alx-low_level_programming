#include "main.h"

/**
 * _abs - computes absolute value of an integer
 *
 * @r: integer to be computed
 *
 * Return: absolute value
 */

int _abs(int r)
{
	if (r < 0)
		r = -(r);
	else
		r = r;
	return ('r');
}
