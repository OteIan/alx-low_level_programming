#include "main.h"

/**
 * _strlen - checks the length of a string
 * @s: string to be checked
 * Return: length
 */

int _strlen(char *s)
{
	int length = _strlen(s);

	_putchar("%c", length);

	return (length);
}
